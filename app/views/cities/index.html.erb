<h1>Layover cities we have so far:</h1>

<% unless @cities.blank? %>

    <% @cities.each do |city| %>
    <li>
        <%= link_to city.name, city %>
    </li>
    <% end %>
    <br>
    <%= link_to "Add a city!", new_city_path %>
<% else %>
No layovers exist yet. <%= link_to "Be the first to add one here!", new_city_path %>
<% end %>
